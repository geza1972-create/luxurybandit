// Canonical login route. The component lives in app/seller/login (kept for history);
// /seller/login now redirects here (see next.config.mjs) so the nice URL is /login.
export { default, dynamic } from "@/app/seller/login/page";
