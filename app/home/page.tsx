// The start page has its own clean URL: /home. It renders the same store page,
// which detects the /home path and shows the "Let's Play Big" feeds grid.
export { default } from "@/app/stores/page";
