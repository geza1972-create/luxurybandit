// Admin mirror of /look/[id]. Same page component; the /admin/ path makes admin mode
// explicit in the URL. The shared component redirects admins here from /look/[id].
export { default } from "@/app/look/[id]/page";
