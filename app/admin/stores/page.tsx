// Admin mirror of /stores. Same page component; the /admin/ path just makes admin
// mode explicit in the URL. The shared component redirects admins here from /stores.
export { default } from "@/app/stores/page";
