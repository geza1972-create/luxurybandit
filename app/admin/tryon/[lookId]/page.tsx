// Admin try-on editor route: same page as /tryon/[lookId], but the /admin/ path makes
// admin mode explicit in the URL. The shared component shows the reference + prompt
// editor when it detects the /admin/ pathname.
export { default } from "@/app/tryon/[lookId]/page";
